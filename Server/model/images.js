// models/Image.js
/*import mongoose from "mongoose";

const imageSchema = new mongoose.Schema({
  images: [String], // or image: String if you're storing one per doc
});

const Image = mongoose.model("Image", imageSchema);
export default Image;*/
